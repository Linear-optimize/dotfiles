version: "3.9"
services:
  rust:
    build: .
    container_name: rust-dev
    volumes:
      - .:/workspace
      
      - cargo-registry:/usr/local/cargo/registry
      - target-cache:/workspace/target
    tty: true
    stdin_open: true

volumes:
  cargo-registry: 
  target-cache: